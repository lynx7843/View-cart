# Base URL of the deployed backend API (Render), including the /api prefix.
# Locally this points at your local server; on Vercel, set it as a project
# env var pointing at your Render URL, e.g. https://view-cart-api.onrender.com/api
VITE_API_URL=http://localhost:5000/api
